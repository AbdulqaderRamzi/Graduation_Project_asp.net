<div style="max-width:75%; text-align:center; margin-left:auto; margin-right:auto;" class="d-flex flex-column justify-content-center align-content-center text-start">
    <h1 class="p-4 my-5 custom-title" style="font-size: 80px; font-weight: 700;">DetailView</h1>
    <h4 class="text-center mt-4 mb-5">Built in class used to display single model object.</h4>
    
    <hr />
    <h2 class="my-5 custom-title">Model</h2>
    <div class="rounded p-4 mb-5 mt-3 custom-div">
        <p class="mb-1">class Book(models.Model):</p>
        <p class="mb-1 mr-25">title = models.CharField(max_length=100)</p>
        <p class="mb-1 mr-25">available = models.BooleanField(default=False)</p>
    </div>

    <hr />
    <h2 class="my-5 custom-title">View</h2>
    <div class="rounded p-4 mb-5 mt-3 custom-div">
        <p class="mb-1">from django.views.generic.edit import DetailView</p>
        <p class="mb-1">from app.models import Book</p>
        <p class="mb-1">class BookDetailView(DetailView):</p>
        <p class="mb-1 mr-25">model = Book # Book.objects.get(id=id)</p>
        <p class="mb-1 mr-25">context_object_name = 'book' # {'book':model}</p>
    </div>

    <hr />
    <h2 class="my-5 custom-title">Url</h2>
    <div class="rounded p-4 mt-3 custom-div">
        <p class="mb-1">from app.views import BookDetailView</p>
        <p class="mb-1">urlpatterns = [</p>
        <p class="mb-1 mr-25">path('book/&lt;int:pk&gt;/', BookDetailView.as_view()),</p>
        <p class="mb-1">]</p>
    </div>
    <h5 class="mt-5 mb-5">It has 4 features.</h5>

    <hr />
    <h2 class="my-5 custom-title">get_context_data</h2>
    <h5 class="mt-3 mb-2">Used to add more context data and change default context name.</h5>
    <h5 class="mt-4 mb-4">Example</h5>
    <div class="rounded p-4 mb-5 custom-div">
        <p class="mb-1">def get_context_data(self, **kwargs):</p>
        <p class="mb-1 mr-25">context = super().get_context_data(**kwargs)</p>
        <p class="mb-1 mr-25">context['book'] = Book.objects.get(id=id)</p>
        <p class="mb-1 mr-25">return context</p>
    </div>

    <hr />
    <h2 class="my-5 custom-title">queryset</h2>
    <h5 class="mt-3 mb-2">Used to define which objects to return.</h5>
    <h5 class="mt-4 mb-4">Example</h5>
    <div class="d-flex justify-content-start mb-1">
        <h5 class="rounded mt-2 mb-4 p-3 custom-back">queryset = Book.objects.get(id=id, available=True)</h5>
    </div>

    <hr />
    <h2 class="my-5 custom-title">template_name</h2>
    <h5 class="mt-3 mb-2">Used to define template name that will be used to display the data.</h5>
    <h5 class="mt-4 mb-4">Example</h5>
    <div class="d-flex justify-content-start mb-1">
        <h5 class="rounded p-3 custom-back">template_name = 'books/book.html'</h5>
    </div>

    <hr />
    <h2 class="my-5 custom-title">get_object</h2>
    <h5 class="mt-3 mb-2">Used to customize how to retrive an object.</h5>
    <h5 class="mt-5 mb-4">Url</h5>
    <div class="d-flex justify-content-start mb-1">
        <h5 class="rounded p-3 custom-back">path('book/&lt;int:bookid&gt;/', BookDetailView.as_view())</h5>
    </div>
    <h5 class="mt-5">View</h5>
    <div class="rounded p-4 mb-5 mt-4 custom-div">
        <p class="mb-1">class BookDetailView(DetailView):</p>
        <p class="mb-1 mr-25">model = Book</p>
        <p class="mb-1 mr-25">context_object_name = 'book'</p>
        <p class="mb-1 mr-25">def get_object(self, queryset=None):</p>
        <p class="mb-1 mr-50"># Retrieve the book using the primary key from the URL</p>
        <p class="mb-1 mr-50">book_id = self.kwargs.get('bookid')</p>
        <p class="mb-1 mr-50">return self.model.objects.get(pk=book_id)</p>
    </div>
</div>